{
  "name": "line_bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "dotenv -- turbo run start --env-mode=loose",
    "start:dev": "dotenv -- turbo run start:dev",
    "build": "dotenv -- turbo run build --env-mode=loose",
    "build:web": "dotenv -- turbo run web#build",
    "build:api": "dotenv -- turbo run api#build",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.4",
    "typescript": "5.7.3"
  },
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*"
  ],
  "description": "Online Shop Demo",
  "author": "m110033@gmail.com",
  "engines": {
    "node": ">=20.x",
    "npm": ">=9.x"
  },
  "dependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
